import React from "react";
import ReactDOM from "react-dom/client";
import InkHiveWebsite from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <InkHiveWebsite />
  </React.StrictMode>
);
